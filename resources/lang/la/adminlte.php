<?php

// The 'la' folder holds the Lao translations, whose ISO 639-1 code is 'lo'.
// The folder is kept as an alias so an application already configured with the
// wrong code keeps working, and it will be removed on the next major release.

return require __DIR__.'/../lo/adminlte.php';
